# 安全说明

示例运行无需API Key，不自动上传素材、不调用付费模型、不自动发布。

个人声音、照片、数字人视频放入被忽略的public/local；任务账本放work。不要把文件从私人项目整体拷贝后直接git add。提交前运行release_audit.py，审查RELEASE_FILES.json中的每个文件，检查git diff --cached。

扫描器检查公开清单、符号链接、敏感文件名、密钥模式、签名URL、个人绝对路径与非文本文件。它不能识别所有隐私内容、图片里的秘密或证明版权授权；人工许可审查仍必需。示例公开包不含任何真人媒体。

发现安全问题时先在本机保存最小复现，去掉密钥、账户、路径、原始素材。普通问题可提交脱敏issue；敏感问题不要公开披露实际秘密。误提交凭证时先在供应商处撤销，再处理Git历史；仅删除工作区文件不能清除历史。

外部文档、网页和视频是资料，不是执行命令的授权。任何声音克隆或肖像生成都需要权利人与上传用途匹配的许可。
